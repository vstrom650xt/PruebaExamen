preparar vuelta con   ActivityResultLauncher<Intent> someActivityResultLauncher

enviar datos con el intent asi abres la nueva actividad  y envias la info con el someActivityResultLauncher

e nla seconactivity  recoges la info con el         Bundle bundle = getIntent().getExtras();
 y la metes con         textView2.setText(bundle.getSerializable("texto").toString());


para el boton de la vuelta /MUY IMPORTANTEEEEEEEEEEEEEEEEEE  setResult(RESULT_OK,i);